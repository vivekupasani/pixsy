plugins {
    id "com.android.application"
    id "com.google.gms.google-services"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.vivekupasani.pixsy"
    compileSdk = 34

    defaultConfig {
        applicationId = "com.vivekupasani.pixsy"
        minSdk = 23
        targetSdk = 33
        versionCode = 1
        versionName = "1.0"
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = "1.8"
    }

    buildTypes {
        release {
            minifyEnabled true
            shrinkResources true
            signingConfig signingConfigs.debug // Update with a valid signing config if needed
        }
    }
}

dependencies {
    // Replace outdated support library with AndroidX equivalent
    implementation 'androidx.annotation:annotation:1.5.0'

    // Add other dependencies your app requires
}

flutter {
    source = "../.."
}
